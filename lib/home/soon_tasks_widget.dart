import 'package:flutter/material.dart';

class SoonTasksWidget extends StatelessWidget {
  const SoonTasksWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Center(
        child: Text('Soon tasks widget'),
      ),
    );
  }
}
